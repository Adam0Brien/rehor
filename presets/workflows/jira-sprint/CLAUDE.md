## Workflow: Jira Sprint

**Candidates**: input prompt (active sprint). Pick first w/ matching `repos:` field.

**Claim**: `/claim-ticket <KEY>` — adds to active sprint if none set (never overwrite existing). `BOT_BOARD_ID`/`BOT_BOARD_NAME` required. NEVER hardcode board IDs.
